const arr = new Array(5);
// console.log(arr);

arr.push(5); //this will append 5 at the end of the array
arr.unshift(5); //this will append a new element at the start of the array.

// arr.shift(); // this will remove the first element from the array

arr[0] = 10; //this will insert the element at the index position of the defined size of the array.

console.log(arr);